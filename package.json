{
  "name": "twikka-convex",
  "private": true,
  "description": "Convex backend for Twikka (Flutter client in the same repo root).",
  "scripts": {
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy"
  },
  "dependencies": {
    "@convex-dev/r2": "^0.10.1",
    "convex": "^1.17.0",
    "svix": "^1.90.0"
  }
}
